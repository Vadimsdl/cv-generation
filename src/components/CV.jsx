import { useLoaderData } from "react-router";
import {Row} from "antd";


const CV = () => {
    const team = useLoaderData();

    return (
        <Row>
            <></>
        </Row>
    );
}

export default CV;